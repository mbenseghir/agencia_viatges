{% extends 'layout.twig' %}

{% block content %}
<section class="panel narrow">
    <span class="eyebrow">Error</span>
    <h1>{{ title|default('S’ha produït un error') }}</h1>
    <p>{{ message|default('No s’ha pogut completar la petició.') }}</p>
    <a class="btn" href="{{ url('home') }}">Tornar a l’inici</a>
</section>
{% endblock %}
