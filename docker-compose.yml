services:
  app:
    build: .
    ports:
      - "8081:80"
    volumes:
      - .:/var/www/html
      - ./database:/var/www/html/database
    environment:
      - APP_ENV=production
    restart: always
